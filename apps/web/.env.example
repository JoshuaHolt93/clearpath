# ClearPath Web (Next.js) — production environment template.
# Set these as Vercel Project Environment Variables.

# Server-side base URL the BFF routes use to reach the FastAPI API.
# Use the Railway service's public URL (or your api subdomain once DNS is cut over).
# No trailing slash.
CLEARPATH_API_URL=https://api.your-domain.com
