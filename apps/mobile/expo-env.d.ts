/// <reference types="expo/types" />

// This file is normally generated/maintained by Expo. It is committed so the
// scaffold's tsconfig `include` resolves before the first `expo start`. Expo may
// overwrite it once the app is activated and run.
